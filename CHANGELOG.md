# 更新记录

这里记录每个版本**用户能感知到的变化**，不罗列代码改动。按版本从新到旧排列；每个版本也在 git 里有同名标记（`git show v0.1.1` 可看到相同说明）。

---

## v0.1.6 · 当前版

这一版来自一次只读审查（2026-09-12，针对 `30baa6c`）复现出的问题。修改前均先复现，再按最小一致的方式修复，并为每条加了用户可见结果的回归测试。

### 扩展版随仓库一起提供

扩展现在直接放在仓库根目录的 `extension/`。以前它只随 Release 的 `sourcepin-0.1.6-delivery.zip` 分发，克隆源码的人要先装 Node.js、跑一次构建才有扩展可加载；现在克隆完就能在 `chrome://extensions` 里「加载已解压的扩展程序」，选择该目录即可。它仍然是构建产物：`npm run build` 重新生成，与 `dist/sourcepin-0.1.6-chrome.zip` 内容逐字节一致，随源码一起提交。

### 敏感信息不再从三个旁路漏出

- **`#fragment` 里的凭据。** 以前只清理查询串，`https://x/#access_token=…` 会原样写进 Meta 的 URL、reach 路径、链接和资源审计。现在查询串、fragment 和 URL 里的用户名密码走同一个净化器：`#access_token=…`、`#/route?token=…`、`#apiKey=…` 都会变成 `[redacted]`；`#section-2`、`#/dashboard/orders` 这类正常锚点和业务路由保持不变；无法可靠解析时整个 fragment 用等长星号占位，不把凭据当作"降级原因"回写。
- **伪元素把已删除的属性读回来。** 元素上的 `data-token` 已从属性快照删除，但 `::before { content: attr(data-token) }` 经浏览器解析后仍把原值写进 CSS 与 Markdown。现在会先静态识别 `attr(...)`，再对仍然存在的"被过滤属性"做一次移除-测量-还原的行为确认；确认由被过滤属性派生的 `content` 整条省略，普通伪元素文案照常保留。
- **Pro 录制的变化详情。** 录制期间 `el.style.setProperty('--api-key', …)` 的值以前会出现在 transitions 里。现在 style 先解析成声明再走共享净化器（新旧两侧同样处理），元素身份（id/class）也只用净化后的值，敏感值只保留原因和计数。

### 取消导出不再被算作"已确认"

以前点「取消」或按 Esc 关掉确认框后，机身仍显示「已确认 · 声明」，下一次导出直接执行。原因是"弹窗展示过"被当成了"用户接受过"。现在两者分开：只有点「继续导出」才记录已确认，并且这份确认只属于当时那一份快照——取消、Esc、关闭工具都不会留下已确认状态，重新选择元素、换目标或页面结构变化后都要重新确认。

### 多选提示不再一闪而过

`Shift 点击可多选` 以前只在悬停标签里出现一帧，第二次绘制就把它换掉，肉眼基本读不到。现在它随悬停出现并停留约 1.6 秒，指针移开即结束，开始新一次选择后可以再出现一次。

### 标签页身份保持不变（已写成回归）

有反馈怀疑"用过 SourcePin 之后，浏览器标签变成了工具的图标和名字"。核查结论：书签版与扩展版运行前后，页面的 `document.title`、整个 `<head>`（含 `<link rel="icon">`）和 URL 都逐字节相同（真实 Chrome 152 + 打包产物实测），代码里也没有任何写标题或写 favicon 的路径；v0.1.4 已经删除的 favicon 注入没有回来。为免以后再退化，端到端套件新增一条断言：在自带标题、favicon、apple-touch-icon 与 canonical 的真实页面上，两个适配器都不得改动标题、head、图标或地址（见 `tests/e2e.mjs`）。

### 边界情况修正

- **开放 ShadowRoot 里的直接文本**不再丢失：`Shadow leading <b>bold</b> trailing` 现在完整保留顺序、转义和字节预算，与普通 DOM 同一条规则。
- **离线图片占位尺寸**改用该位置快照记录的矩形，不再读脱离文档的 `offsetWidth`（在惰性 template 里恒为 0），也不再 `parseFloat` 把 `50%`、`10rem` 当成像素；自然尺寸 320×180 的图片不会再变成 80×40。
- **ShadowRoot 内的同源 iframe** 现在能被拾取：以前它既不进入取景扫描，点击又会直接执行原网页的 handler。现在开放 shadow root 会递归扫描，跨源限制不变，创建/移除循环不会累积监听器。

### 键盘焦点可见

机身上的隐形热区以前完全不画焦点框（连浏览器默认的也去掉），Tab 用户看不出焦点在哪。现在焦点框改为贴在各自图形周围（面板里仍是白框），只在 `:focus-visible` 下出现，纯鼠标点击依旧不画框；「已确认 · 声明」有了独立的键盘入口（不再是嵌套按钮）；拖动条聚焦后可用方向键移动机身、Home 复位。新增的测试改为断言用户可观察的键盘焦点，而不是"outline 必须为 none"。

### 构建锁不再有初始化竞争

两个构建进程同时运行时，A 建好锁目录、还没写 pid 的一瞬间，B 会把它当成死锁删掉，两边都以为拿到了锁；A 退出时的清理还可能删掉 B 的新锁。现在锁有明确的初始化状态和宽限期、带所有者的令牌，接管用原子 rename 完成（只有一个进程能抢到），释放前先核对所有权；被强杀的旧锁仍能恢复，损坏的锁目录不会让构建卡死。构建产物依旧原子发布，SHA256SUMS 与本次构建的产物一致。

---
## v0.1.5 · 上一版

### 每个按钮跳自己那一个

点齿轮时跳的是蓝色按钮、点蓝色按钮时跳的是齿轮——两个按钮的功能对调过，但按压动画还指着旧的图形。现在每个按钮按下去，动的都是它自己：齿轮、蓝圆、黄色 M、粉色圆钮、三角。

### 点完按钮再按 Esc，不再冒出白色外框

鼠标点击并不会显示焦点框，但紧接着按 Esc，浏览器会把输入方式切回键盘，那个刚点过的按钮立刻被画上一圈白色描边——看起来像被选中了。机身自己的控件现在完全不画焦点框（连浏览器默认的那圈也去掉）。面板里的按钮保留，因为那里确实要用键盘操作。

### 黄色 M 上移自身高度的 1/5

M 字形连同它的可点区域一起上移，点击位置仍然对准图形；按下时的动画也在新位置上播放，不会先掉回原位。

### 扩展注入失败时不再冤枉页面

在普通网页上唤起偶发失败时，扩展以前会说「此页面不允许注入。请在普通 HTTP/HTTPS 网页使用 SourcePin。」——而那个页面就是普通网页。现在：

- 只有 Chrome 明确表示页面受保护时（浏览器内置页、扩展商店页等）才这样提示；
- 其它情况直接给出真实原因，并提示可以再点一次；
- 注入失败会自动重试一次，很多偶发失败因此不再发生。

---

## v0.1.4 · 上一版

### 取消「书签图标注入」

以前在网页上唤起书签版时，工具会顺手把当前页面的图标换成 SourcePin 的方形图标，指望浏览器把它记到书签栏那一项上。这解决不了问题：`javascript:` 书签没有可抓取的站点图标，书签栏显示哪个图标由浏览器自己决定。该功能已移除。

- SourcePin 不再改动任何页面的图标，包括 `<link rel="icon">`。
- 书签栏那一项显示什么，和以前一样由浏览器决定，工具不做保证，也不再给「删掉再拖一次」这类无效建议。
- 安装页上的 SourcePin 标签本身仍带方形图标（那是页面上的标记）；扩展版的工具栏按钮与扩展管理页图标不变。

### 文档

- 安装页与 README 里关于书签图标的说法改成实际情况，不再暗示可以让书签栏显示方形图标。
- v0.1.1 曾把图标注入记为本版卖点，此处按现行为取消。

---

## v0.1.3 · 上一版

### 模式文字跟着开关一起出现、一起消失

机身下方的 `LITE` / `PRO` 现在只在点绿色图标、Switch 出现时显示；收起 Switch 后机身下方不留任何文字。

- Switch 出现 → 模式文字同时出现；Switch 收起 → 同时消失。
- 切换 Lite / Pro 时，这段文字同步变成 `PRO` / `LITE`。
- 读者仍能通过按钮的无障碍名称听到当前模式（「选择 Lite 或 Pro 模式 · 当前 Lite」）。

v0.1.2 把这段文字从机身底边移到了机身下方，但它仍然是常驻的——开关收起后单独留在那里，看起来像没擦干净的残留文字。本版改为与开关同步。

---

## v0.1.2 · 上一版

### 安装页新增键盘快捷键说明

安装页多了一节「键盘快捷键」，六个键一次讲清：`Shift` 加选、`⌘/Ctrl+C` 复制摘要、`⌘/Ctrl+Shift+C` 复制并截图、`⌘/Ctrl+Shift+M` 下载、`Esc` 一次收起一层、`Tab` 到屏幕后按 `Enter` 打开预览。

同时列出**什么情况下按键不会生效**：还没选组件、光标停在输入框里、页面上已经选中了文字、组合被浏览器或其它扩展占用、以及书签版本身没有全局快捷键。这些以前都得按下去才知道。

### 机身下方的模式文字不再压住机身

`LITE` / `PRO` 原本压在机身底边上，被描边横穿，看起来像被裁掉的乱码。现在完整显示在机身下方；展开 Lite / Pro 开关时它会自动让位给开关自己的标签，不会出现两份模式文字。

### 安装页刷新时不再偶发图片裂开

构建改为原子替换：构建进行当中刷新安装页，不会再读到写了一半的文件（此前表现为展示区的游戏机图片偶尔变成裂图，刷新一次又正常）。

### 术语统一

界面与文档里的「掌机」全部改为「游戏机」。

---

## v0.1.1 · 上一版

### 导出不再因为图片失败

图片位置显示同尺寸的灰色占位，图片本身不再下载、不再内联进导出文件。

- 含大量图片的页面也能正常导出；ZIP 体积不随图片变大。
- `assets.json` 记录每个图片位置的原始地址，仍然可以回溯到图片在哪。
- 离线打开 `page.html` 时，版面与图片位置都保留，只是显示占位而非原图。

### 导出确认不再反复打断

每次唤起工具后只完整弹一次确认，之后同一次会话内直接执行。

- 首次复制、下载或截图照旧显示完整确认（个人信息计数、第三方处理提示、权利声明）。
- 之后不必反复点「继续导出」。
- **只要快照里检测到个人信息，仍会重新弹出完整确认**——这条没有放宽。

### 声明随时可见

确认过的会话在屏幕底部显示「已确认 · 声明」，点它可以随时重看完整声明。没有选中内容时屏幕只保留笑脸，不残留文字。

### Esc 一次只收起一层

- 第一下：只关闭打开的面板，**选择和已采集结果都保留**。
- 第二下：取消选择。
- 第三下：退出工具。
- 在导出确认框上按 Esc 只取消本次导出，选择仍在，可以马上再导出一次。

### 按钮不再「点了才知道不行」

- 书签版与安装页里，「截取组件」「截取当前视口」置灰，并在面板内写明需要 Chrome 扩展版。
- Lite 下的「开始录制」变成一步到位：点一次即切到 Pro 并开始录制，按钮文字同步显示为「切换到 Pro 并开始录制」。
- 未选择元素时点「追加当前视口」，会提示先选择一个元素。

### 状态看得见

- 当前模式常驻显示在机身下方（`LITE` / `PRO`），不再只靠机身颜色区分。
- 多选达到 10 个上限时给出提示，而不是静默丢弃超出的那一次点击；首次悬停会提示 Shift 可多选。
- 面板配色跟随模式：Lite 青绿、Pro 粉红，与机身一致。

### 新的方形图标

图标取自 Lite 状态的屏幕（青绿机身框、浅色屏面、笑脸），做成正方形，四个尺寸 16/32/48/128。

- 扩展版：工具栏按钮、扩展管理页统一使用。
- 书签版：唤起时会把自己的图标写入页面，书签栏不再显示通用地球图标。**已装好的旧书签不会自动更新，删掉再拖一次即可。**

### 安装页讲清楚了怎么用

新增「游戏机上每个按钮做什么」一节：逐个说明六个按钮的作用，以及设置、模式切换、画面采集、预览、导出确认、保存截图各弹窗里每一项的含义，另附常见提示的解读。

---

## v0.1.0 · 上一版

### 已经可用

- 整页 DOM 导出：结构、样式、定位线索一起打包成可离线打开的 ZIP。
- 复制得到 15 KB 摘要（粘贴给 AI 的那段），下载得到完整报告。
- 预览窗与机身等大，位于机身上方。
- 使用引导只在安装页首次体验和扩展首次使用时出现一次。
- Pro 主题在限制 CSP 的页面上也能正确显示红色机身。

### 已知会让人困惑的地方

这些在 v0.1.1 中修正：

- Esc 一按就同时关面板、清空选择、丢掉已采集结果。
- 每次复制都要过一遍完整确认。
- 书签版里截图按钮点了才知道不可用。
- Lite / Pro 只能靠机身颜色区分。
- 离线 ZIP 的图片以 base64 内联，图片多的页面可能大到无法导出。
