# P1 离线包与合规产物实施记录

根据用户本轮任务书继续，不修改历史设计文件。

- [x] 审计与文案：safeAttributes 返回属性和移除名称；版本/权利元数据；中性文案；README 权利说明、许可证和商店披露待办；个人信息模式计数；摘要精确丢弃清单。完整 check。
- [x] 离线导出：page 下载 ZIP（page.html、结构 JSON、Markdown、资源结果清单）；图片按需无凭据 GET 并内联，下载失败占位；离线 CSP 阻断其他网络依赖；总包、单资源、下载数量和耗时均有界；导出/复制/截图前本地审核确认。完整 check。
- [x] P2：在现有 120 节点采样上限内建立安全候选索引，样式对象去重及合并 CSS；500 节点或时间片让出；自身/祖先透明状态分开报告。完整 check。
- [x] 验收：固定长页在书签和扩展实际下载 ZIP，解包后以 file URL 离线打开，关键节点、图片、shadow/template、字节预算和无外部请求实测；更新报告和产物。

体积策略：ZIP 容器不压缩，保证浏览器无依赖实现；单页资源内联 HTML 上限 8 MiB、资源原始下载累计 4 MiB/单个 2 MiB/最多 80 个/总耗时 20 秒，完整 ZIP 16 MiB、单份 Markdown 4 MiB。超过硬上限明确拒绝并要求缩小范围，不静默漏文件。失败资源清单只记录净化 URL 和原因。
